{
  "name": "simple-q",
  "version": "1.0.0",
  "description": "Simple lightweight microservice for queuing data with data separation for different microservices. Support weighted queue.",

  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "fastify": "^4.13.0"
  }
}
